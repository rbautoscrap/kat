# Prefer Dockerfile (Node 20). If image build fails in UI, switch builder to NIXPACKS.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node scripts/start-prod.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
